import './index.less'

//{['aaa', 'bbb', {aaa: true}]}
import events from 'events'

const bad = true
const aaa = (
	<div className='ssss fds'>
		<div aaa={`${styles.name} ${styles.age}`}>Home - aaaaa</div>
		<div>{events.on}</div>
	</div>
)
